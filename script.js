/**
 * Función sencilla para manejar los clics de los botones
 * @param {string} nombreAccion - El nombre de la acción realizada
 */
function ejecutarAccion(nombreAccion) {
    console.log(`Botón presionado: ${nombreAccion}`);

    // Crear un mensaje visual para el usuario
    alert(`¡Has seleccionado la opción: ${nombreAccion}!`);

    // Podríamos añadir más lógica aquí, como cambiar estilos o navegar
}
